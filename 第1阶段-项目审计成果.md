# 第1阶段：项目审计成果

## 一、项目架构图

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户操作层                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │  微信客户端   │  │  Python脚本  │  │  Web Dashboard        │   │
│  │  (Windows/macOS)│  │  CLI工具    │  │  (浏览器访问)         │   │
│  └───────┬──────┘  └───────┬──────┘  └───────────┬──────────┘   │
└──────────┼─────────────────┼─────────────────────┼──────────────┘
           │                 │                     │
           ▼                 ▼                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据提取层                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  wechat-decrypt (外部依赖)                                │  │
│  │  - 提取微信加密密钥 (find_all_keys_macos)                 │  │
│  │  - 解密 SQLite 数据库 (decrypt_db.py)                     │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据存储层                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  解密后的数据库                                            │  │
│  │  ├── contact/contact.db (联系人/群聊信息)                 │  │
│  │  └── message/message_*.db (消息记录)                      │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        分析模块层                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  wechat-stats.py (核心分析脚本)                           │  │
│  │  - 构建联系人映射 (build_contact_map)                     │  │
│  │  - 群聊分析 (analyze_group)                               │  │
│  │  - 成员活跃度标签分类                                      │  │
│  │  - 输出 JSON 格式结果 (wechat-stats.json)                │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        展示层                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  wechat-server.py + dashboard.html                        │  │
│  │  - HTTP 服务器 (http.server)                              │  │
│  │  - API 接口 (/api/run, /api/status)                       │  │
│  │  - 交互式 Dashboard 页面 (深色主题)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 输入输出文件清单

| 文件类型 | 路径/说明 |
|---------|---------|
| **输入文件** | 微信客户端加密数据库 (由 wechat-decrypt 处理) |
| **解密后数据库** | `wechat-decrypt/decrypted/contact/contact.db` (联系人/群聊)<br>`wechat-decrypt/decrypted/message/message_*.db` (消息) |
| **配置文件** | `group-names.json` (自定义群聊名称映射) |
| **输出文件** | `wechat-stats.json` (分析结果 JSON) |
| **展示文件** | `dashboard.html` (Web 可视化页面) |

### 1.3 核心模块功能

| 模块 | 文件 | 功能 |
|-----|------|-----|
| **解密引擎** | wechat-decrypt | 从微信客户端提取密钥并解密数据库 |
| **分析脚本** | wechat-stats.py | 从解密数据库提取数据，统计群聊活跃度，生成 JSON |
| **Web 服务** | wechat-server.py | 提供 HTTP 服务，托管 Dashboard，提供 API 接口 |
| **Dashboard** | dashboard.html | 展示分析结果，支持搜索、排序、筛选 |

---

## 二、完整流程图

### 2.1 数据处理全流程

```
┌─────────────────┐
│   1. 微信客户端  │
│   (运行中)       │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  2. 提取密钥 (需要 sudo 权限)        │
│     find_all_keys_macos             │
│     → all_keys.json                 │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  3. 解密数据库                       │
│     decrypt_db.py                   │
│     → decrypted/contact/contact.db  │
│     → decrypted/message/message_*.db│
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  4. 构建联系人映射                   │
│     build_contact_map()             │
│     - contact_id → display_name     │
│     - wxid → display_name           │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  5. 读取群聊列表                     │
│     SELECT id, username FROM chat_room│
│     WHERE username LIKE '%@chatroom'│
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  6. 分析单个群聊 (analyze_group)    │
│     ┌───────────────────────────┐  │
│     │ a. 获取群成员列表          │  │
│     │ b. 匹配 sender_id ↔ wxid  │  │
│     │ c. 统计各时间段发言数      │  │
│     │ d. 打标签 (🔥超活跃等)    │  │
│     └───────────────────────────┘  │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  7. 生成 wechat-stats.json          │
│     {                                │
│       "generated_at": "...",        │
│       "groups": [                   │
│         {                            │
│           "name": "群名",            │
│           "total_members": 100,     │
│           "total_messages": 5000,   │
│           "active_1month": 30,      │
│           "never_spoken": 40,       │
│           "all_members": [...]      │
│         }                            │
│       ]                              │
│     }                                │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  8. 启动 Web 服务                   │
│     wechat-server.py → :8080        │
└────────┬────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  9. 访问 Dashboard                  │
│     http://localhost:8080/dashboard.html│
│     - 群选择器                       │
│     - 统计卡片                       │
│     - 活跃度分布图                   │
│     - 成员排行表                     │
└─────────────────────────────────────┘
```

### 2.2 成员标签分类逻辑

```
近1月发言数 ≥20 → 🔥超活跃
近1月发言数 ≥5  → 🟢活跃
近3月发言数 ≥5  → 🟡偶尔
近6月发言数 >0  → 🟠低频
历史发言数 >0   → 🔴沉水
从未发言        → 💀死号
```

---

## 三、风险清单

### 3.1 兼容性风险

| 风险项 | 说明 | 严重程度 |
|-------|------|---------|
| **macOS 专属** | 密钥提取工具 `find_all_keys_macos` 仅支持 macOS，Windows 用户无法直接使用 | 🔴 高 |
| **微信版本更新** | 微信客户端版本更新可能导致加密方式变化，需要重新适配解密工具 | 🟡 中 |
| **Python 依赖** | 需要安装 Python 3 及相关依赖，环境配置复杂 | 🟡 中 |
| **数据库格式** | 不同微信版本的数据库表结构可能存在差异 | 🟡 中 |

### 3.2 用户操作门槛风险

| 风险项 | 说明 | 严重程度 |
|-------|------|---------|
| **命令行操作** | 用户需要使用 Terminal/命令提示符执行多条命令，对普通用户不友好 | 🔴 高 |
| **GitHub 依赖** | 需要从 GitHub 克隆两个项目，对技术小白不友好 | 🔴 高 |
| **微信重签名** | macOS 用户需要对微信进行重签名操作，步骤繁琐 | 🟡 中 |
| **sudo 权限** | 提取密钥需要 sudo 管理员权限 | 🟡 中 |
| **多步骤流程** | 从安装到获取结果需要 5+ 个步骤，容易出错 | 🔴 高 |

### 3.3 技术限制

| 限制项 | 说明 |
|-------|------|
| **本地数据** | 只能分析用户自己的微信本地数据，无法远程获取 |
| **实时性** | 需要用户手动运行脚本刷新数据，不支持实时更新 |
| **单设备** | 仅能分析当前登录设备的微信数据 |

---

## 四、当前成果总结

✅ **已完成**：
- 完整克隆并分析了开源项目代码
- 理解了项目架构和数据流程
- 识别了核心功能模块
- 整理了风险清单

📋 **关键发现**：
1. 项目依赖外部 `wechat-decrypt` 工具进行解密
2. 当前仅支持 macOS 平台
3. 用户操作门槛高，不适合闲鱼普通用户
4. 输出 JSON 格式，可进一步包装成报告

---

## 五、是否达到阶段目标

✅ **是**，已完成第1阶段全部要求：
- ✅ 输出项目架构图
- ✅ 输出完整流程图
- ✅ 输出风险清单

---

## 六、下一阶段计划

**第2阶段：用户导出流程设计**

目标：
1. 设计 Windows 和 macOS 两个版本的用户导出流程
2. 制作详细的操作步骤文档（图文并茂）
3. 绘制完整的用户旅程图（从购买到获得报告）

重点关注：
- 避免命令行操作
- 简化步骤
- 使用图形化工具
