# 第4阶段：评分系统设计

## 一、评分公式设计

### 1.1 综合评分公式

```
综合评分 = (活跃率 × 0.35) + (死号比例 × 0.25) + (近期活跃 × 0.25) + (发言均衡度 × 0.15)
```

### 1.2 各项指标定义

| 指标 | 权重 | 计算方式 | 说明 |
|-----|------|---------|-----|
| 活跃率 | 35% | (发过言的成员数 / 总成员数) × 100 | 越高越好 |
| 死号比例 | 25% | (从未发言的成员数 / 总成员数) × 100 | 越低越好（反向计分） |
| 近期活跃 | 25% | (近1个月活跃成员数 / 总成员数) × 100 | 越高越好 |
| 发言均衡度 | 15% | 1 - (基尼系数) | 越均衡越好 |

---

## 二、各指标详细计算公式

### 2.1 活跃率计算

```
活跃率 = (发过言的成员数 ÷ 总成员数) × 100%

示例：
- 总成员数：128人
- 发过言的成员：92人
- 活跃率 = (92 ÷ 128) × 100% = 71.9%

得分：71.9分（直接使用百分比）
```

### 2.2 死号比例计算（反向计分）

```
死号比例 = (从未发言的成员数 ÷ 总成员数) × 100%

反向计分项：
死号得分 = 100 - 死号比例

示例：
- 总成员数：128人
- 从未发言的成员：36人
- 死号比例 = (36 ÷ 128) × 100% = 28.1%
- 死号得分 = 100 - 28.1 = 71.9分
```

### 2.3 近期活跃计算

```
近期活跃率 = (近1个月活跃成员数 ÷ 总成员数) × 100%

示例：
- 总成员数：128人
- 近1个月活跃成员：42人
- 近期活跃率 = (42 ÷ 128) × 100% = 32.8%

得分：32.8 × 2.5 = 82分（归一化处理）
```

### 2.4 发言均衡度计算（使用基尼系数）

```
步骤1：按发言数对成员进行排序
步骤2：计算累积发言占比
步骤3：计算基尼系数 G = 1 - 2 × 洛伦兹曲线下面积
步骤4：发言均衡度 = 1 - G

基尼系数取值范围：0（完全均衡）到 1（完全不均衡）
发言均衡度取值范围：0（最不均衡）到 1（最均衡）

示例：
- 基尼系数 = 0.35
- 发言均衡度 = 1 - 0.35 = 0.65
- 得分 = 0.65 × 100 = 65分

简化计算方法（无需复杂基尼系数）：
发言均衡度 = 1 - (前10%成员发言占比 - 10%)

如果前10%成员发言占比为60%：
发言均衡度 = 1 - (0.60 - 0.10) = 0.50
得分 = 50分
```

---

## 三、综合评分计算示例

### 3.1 示例群聊数据

| 指标 | 数值 | 得分 | 权重 | 加权得分 |
|-----|------|-----|------|---------|
| 活跃率 | 71.9% | 71.9分 | 0.35 | 25.17分 |
| 死号比例 | 28.1% | 71.9分 | 0.25 | 17.98分 |
| 近期活跃 | 32.8% | 82分 | 0.25 | 20.5分 |
| 发言均衡度 | 0.65 | 65分 | 0.15 | 9.75分 |
| **合计** | - | - | **1.0** | **73.4分** |

### 3.2 最终评分结果

```
综合评分 = 25.17 + 17.98 + 20.5 + 9.75 = 73.4分

等级：良好（70-89分）
星级：⭐⭐⭐⭐☆
```

---

## 四、评分等级定义

### 4.1 等级划分标准

| 分数区间 | 等级 | 星级 | 评价 | 建议 |
|---------|-----|------|-----|-----|
| 90-100分 | 优秀 | ⭐⭐⭐⭐⭐ | 🌟 运营非常出色！ | 保持现状，继续优化 |
| 70-89分 | 良好 | ⭐⭐⭐⭐☆ | 运营状况良好 | 小幅优化即可 |
| 50-69分 | 一般 | ⭐⭐⭐☆☆ | 需要关注 | 针对性改进 |
| 0-49分 | 危险 | ⭐⭐☆☆☆ | ⚠️ 需要紧急处理 | 全面优化 |

### 4.2 各等级详细说明

#### 优秀（90-100分）
```
特征：
- 活跃率 85% 以上
- 死号比例 10% 以下
- 近期活跃 50% 以上
- 发言均衡度 0.75 以上

示例群：
- 总成员：100人
- 活跃率：90%
- 死号比例：5%
- 近期活跃：60%
- 发言均衡度：0.82
- 综合评分：92分
```

#### 良好（70-89分）
```
特征：
- 活跃率 60-84%
- 死号比例 10-30%
- 近期活跃 30-49%
- 发言均衡度 0.5-0.74

示例群：
- 总成员：128人
- 活跃率：71.9%
- 死号比例：28.1%
- 近期活跃：32.8%
- 发言均衡度：0.65
- 综合评分：73分
```

#### 一般（50-69分）
```
特征：
- 活跃率 40-59%
- 死号比例 30-50%
- 近期活跃 15-29%
- 发言均衡度 0.3-0.49

示例群：
- 总成员：200人
- 活跃率：45%
- 死号比例：40%
- 近期活跃：20%
- 发言均衡度：0.40
- 综合评分：58分
```

#### 危险（0-49分）
```
特征：
- 活跃率 40% 以下
- 死号比例 50% 以上
- 近期活跃 15% 以下
- 发言均衡度 0.3 以下

示例群：
- 总成员：150人
- 活跃率：30%
- 死号比例：55%
- 近期活跃：10%
- 发言均衡度：0.25
- 综合评分：35分
```

---

## 五、评分计算代码实现

### 5.1 Python 评分计算函数

```python
def calculate_health_score(group_data):
    """
    计算群健康度评分
    
    参数:
        group_data: 群数据字典
            {
                'total_members': 总成员数,
                'active_members': 发过言的成员数,
                'dead_members': 从未发言的成员数,
                'recent_active': 近1个月活跃成员数,
                'message_distribution': 发言分布数据（可选）
            }
    
    返回:
        {
            'total_score': 综合评分,
            'grade': 等级,
            'stars': 星级,
            'breakdown': 各项指标得分
        }
    """
    total_members = group_data['total_members']
    
    # 1. 活跃率 (权重 35%)
    active_rate = (group_data['active_members'] / total_members) * 100
    active_score = active_rate
    
    # 2. 死号比例 (权重 25%)
    dead_rate = (group_data['dead_members'] / total_members) * 100
    dead_score = 100 - dead_rate
    
    # 3. 近期活跃 (权重 25%)
    recent_rate = (group_data['recent_active'] / total_members) * 100
    recent_score = min(recent_rate * 2.5, 100)  # 归一化到100分
    
    # 4. 发言均衡度 (权重 15%)
    if 'message_distribution' in group_data:
        balance_score = calculate_balance_score(group_data['message_distribution'])
    else:
        # 简化计算，假设中等均衡度
        balance_score = 60
    
    # 综合评分
    total_score = (
        active_score * 0.35 +
        dead_score * 0.25 +
        recent_score * 0.25 +
        balance_score * 0.15
    )
    total_score = round(total_score, 1)
    
    # 确定等级
    if total_score >= 90:
        grade = '优秀'
        stars = '⭐⭐⭐⭐⭐'
    elif total_score >= 70:
        grade = '良好'
        stars = '⭐⭐⭐⭐☆'
    elif total_score >= 50:
        grade = '一般'
        stars = '⭐⭐⭐☆☆'
    else:
        grade = '危险'
        stars = '⭐⭐☆☆☆'
    
    return {
        'total_score': total_score,
        'grade': grade,
        'stars': stars,
        'breakdown': {
            'active_rate': {'value': round(active_rate, 1), 'score': round(active_score, 1)},
            'dead_rate': {'value': round(dead_rate, 1), 'score': round(dead_score, 1)},
            'recent_active': {'value': round(recent_rate, 1), 'score': round(recent_score, 1)},
            'balance': {'score': round(balance_score, 1)}
        }
    }


def calculate_balance_score(message_distribution):
    """
    计算发言均衡度得分
    
    简化算法：
    - 前10%成员发言占比越高，均衡度越低
    """
    total_messages = sum(message_distribution)
    if total_messages == 0:
        return 0
    
    # 按发言数降序排序
    sorted_messages = sorted(message_distribution, reverse=True)
    top_10_percent = int(len(sorted_messages) * 0.1)
    
    if top_10_percent == 0:
        return 100
    
    top_messages = sum(sorted_messages[:top_10_percent])
    top_ratio = top_messages / total_messages
    
    # 理想情况是前10%成员发言占10%
    balance_degree = 1 - (top_ratio - 0.1)
    balance_degree = max(0, min(1, balance_degree))
    
    return balance_degree * 100


# 测试示例
if __name__ == '__main__':
    test_group = {
        'total_members': 128,
        'active_members': 92,
        'dead_members': 36,
        'recent_active': 42,
        'message_distribution': [1256, 1089, 923, 756, 689] + [100]*88 + [0]*35
    }
    
    result = calculate_health_score(test_group)
    print(f'综合评分: {result["total_score"]}分')
    print(f'等级: {result["grade"]} {result["stars"]}')
    print('各项得分:')
    for key, value in result['breakdown'].items():
        print(f'  {key}: {value}')
```

---

## 六、评分系统优化建议

### 6.1 可扩展指标

后续可考虑增加以下指标：
- 消息质量（平均消息长度）
- 互动频率（回复率）
- 内容多样性（话题分布）
- 增长趋势（环比/同比）

### 6.2 权重动态调整

可根据群类型调整权重：
- 工作群：提高近期活跃权重
- 兴趣群：提高发言均衡度权重
- 交易群：提高活跃率权重

---

## 七、当前成果总结

✅ **已完成**：
- 评分公式设计
- 各指标详细计算方式
- 等级划分标准
- Python 代码实现
- 示例计算

---

## 八、是否达到阶段目标

✅ **是**，已完成第4阶段全部要求：
- ✅ 输出评分公式
- ✅ 包含活跃率、死号比例、近期活跃、发言均衡度
- ✅ 定义各等级（优秀/良好/一般/危险）
